# 设计裁决与推演记录（AGENTS.md 档案层）

> 本文是 AGENTS.md 的**裁决档案**：行为铁律在 AGENTS.md（每 session 载入），裁决理由、推演过程、事故复盘在此（按需查阅）。
> 分层依据（2026-09-18 系统性推演 P1）：AGENTS.md 是纪律物件（绳的合法组成），但内容混入了判决史——注入面只放规则，理由外移（T10 精神对绳自身的适用）。
> 各层权威设计文档不变：基石 = `多人AI协作机制_方案基石.md`；v0.3 = `OuSheng_工程本体化改造方案_v0.3.md`；v0.4 = `OuSheng_共识层设计方案_v0.4.md`。

---

## 1. 全局审计结论（2026-09-18 系统性推演 + 重推演）

**标尺**（用户裁决，2026-09-18）：绳的定位不动——绳是引导 + 卡点，不能变重，主要卡大模型的能力（LLM + git + 约定顶得住的都还给 LLM）。

### 1.1 卡点不变式（从全部写路径门与"不卡"清单中归纳）

> **卡 agent 的写，不卡 human 的手；卡跨主体伤害与状态合法性，不卡语义与质量。**

- 所有写路径门（C1/C2/decide 门/active 门/F4/resolve-owner 门）都长在 CLI 写径上；human 手改 YAML 可绕过是有意设计，外防线 = git 审计。
- "不卡"清单同样齐整且各有记录在案的理由：信息完备性不设硬门（机器测不了语义，硬门逼出"待定"式造假）；声部伪造不机器校验（advisory，git author 对账）；evidence 除 git_commit 外不验真（传感器属消费方局部环，绳建验证管线 = 替牛使劲）；architecture 无门禁（文档错误成本 = 人看见改回来）。

### 1.2 审计发现（P1–P7）

| # | 发现 | 处置 |
|---|---|---|
| P1 | AGENTS.md 混入判决史，18.6KB 每 session 注入税 ≈ 6–9k token，且与 docs/ 重复（双份腐烂面） | 本次分层修复 |
| P2 | WorkItem 字段集从未跑移除轴（基石卡 6 字段 → 现约 20） | 见 §7 字段移除轴裁决 |
| P3 | Contract 挂 WorkItem 的长期张力：接口比单长寿，跨多单时"谁是当前权威"靠约定不靠结构 | 现判决维持（升顶层实体 = 加注册表 = 变重）；激活判据：真实跨单接口权威漂移事故 |
| P4 | 剧本线性增长税 | 判据入 AGENTS.md：新剧本 = 真实事故/推演复盘驱动，禁预防性编写 |
| P5 | v0.3 定位句"1~3 人"与现实多机拓扑 | 用户裁决：定位不动，维持原文 |
| P6 | evidence 类型不在 `work show` 一跳内（下游判 verified 可信度需要看证据类型） | 已修（work show 显示 evidence 清单） |
| P7 | `ousheng todo` 隐式建 assignment——绳替人填数据的极小"替牛"手势 | 可接受（单人最短开工的刻意 UX，唯一 human/系统前提兜底）；观察项，不动作 |

### 1.3 runtime 审计结论

逐部件过"卡什么/删了 LLM 顶得住吗/是引导还是拉车"三问：24 命令 + 全部 internal/ 中**没有拉车部件**（无生成器、语义合并、reasoner、自动催办）；cmd/ousheng 摊薄 ≈ 每文件 90 行。重量全部集中在**非运行时层**：AGENTS.md 文本（P1）、测试/剧本基建（P4）、sqlite 可选件（已有激活判据）。定位在代码层守住。

---

## 2. 发布条件（2026-09-18 推演全文）

### 2.1 三条件推导

一个发布出去的任务要成立，必须**可寻址**（落在某个系统）、**可问责**（有主，或显式标记待认领）、**可判定**（信息够执行者开工、够验收者判定）。缺一即不可执行——现状的反面不是"所有人都会执行"，而是"**没人执行**"：无主单不进任何人的 `context me`，静默孤儿。

### 2.2 状态即门槛（信息门槛挂状态，不挂创建）

反事实：信息门挂创建 → 建单摩擦 → 逼出"待定"式造假（description 填垃圾过门）。故 `backlog` 不查（先记账），`ready` 起查按类型最小信息集，`done` 必有证据（C1）。

### 2.3 active 无主硬门的统一（2026-09-18 定位推演）

- 三态定义皆为"有人正在推进"：doing 有人在干、testing 有人在验、blocked 有人会回来解阻。无主 = 语义矛盾，且没有任何执行者的 context me 会收到它。
- 合法场景（换人验证、换人接手）走 `work assign` **换主**，从不需要清空主 → 无为 testing/blocked 开特例的理由。
- 反事实：开特例 → 多分支多解释，还给"清空主逃避问责"留缝。
- 硬门定位：跨主体伤害（无人收到 ≠ 无害，是协作假象），与 C1/C2 同族；写路径硬门 + converge BLOCKER 双层。

### 2.4 分层强制

硬门只挡"进行中无主"；其余走曝光——converge：active 无主 = BLOCKER、backlog/ready 无主 = warning「待认领」、ready+ 缺信息 = warning。「信息够不够」有判断成分，硬门会逼出造假（同 2.2 反事实）。

### 2.5 四路判决（无主/信息不全信号）

不进 `context me`（注入面最小化——执行者不需要知道不归他的单）；进 `work list` + converge。`view system` 只列 active，不是无主信号面（写路径已杜绝 migrate 之外的无主 active）。

### 2.6 migrate 与无主 active

v1 `live` 卡迁移为 `doing`；owner 解析失败标 `migration_status: needs_resolution` 且无主 → converge BLOCKED（有意：无主 active 不可执行）。仅 `live` 卡——`proposed`/`agreed` 落 ready/backlog，无主仅 warning「待认领」。指定解阻路径 `migrate resolve-owner` 与 Service.Update 同门（active 且无 assignee 拒绝；role-only 解析会留下无主 active，必拒）。

### 2.7 修复记录

- `3b3a76a`：文档改真（`view system` 假承诺删除）+ `--unassigned` 隐含 open + active 门从 doing 扩三态。
- `5acd684`：二次 review——resolve-owner 补 active 门（role-only 绕道洞）+ `TestMigratedNeedsResolutionBlocks` 换真链锁（converge 不读 MigrationStatus，手构 item 与 doing 用例等价，锁不住链路）。
- **教训（入 §6 教训集）**：同语义门必须覆盖全部写径——扩门时只查了 Service.Update，漏了 migrate 直写。

---

## 3. SQLite 定位（2026-09-13 推演裁决，防"零读者部件"复发）

- 产品查询路径默认 memory（零基础设施模式）；sqlite 是派生索引。
- 树内消费者 = S5 等价测试（交叉验证 oracle）；树外用途 = 只读 SQL 查询面（`ousheng index rebuild` 后查 `.ousheng/cache/index.db`）。
- 接入查询路径的激活判据与升 authoritative store 相同（v0.3 §1）：真实使用证明 memory 每命令重建成为实际痛点，才接。

---

## 4. 两段式配置 join（2026-09-15 三轮推演）

### 4.1 死法机理（永久出局）

- **join 审批门**：无效门——有 push 权者拦不住（他能直接 push），无权者本来进不来（没 push 权注册了也没用）；federation 退回中心规划。
- **注册表自动清理**：破坏性动作须 human。
- **responsible-human 活跃度检查**：语义，机器测不了。
- **Web 配置中心/注册服务**：零基础设施违例。
- **repos.yaml 集中上绳**：本机路径非共识，状态/共识两分判出局。

### 4.2 时间轴接力

join 管初始注册（事实）→ design 管拓扑演化（共识，动土先起 design）→ work 管执行（状态），三段无缺口。

---

## 5. 剧本档案

| 剧本 | 拓扑 | 覆盖 |
|---|---|---|
| `dogfood-s7.sh` | 四机（PM/后端/前端/测试 + bare 中央仓） | 全流程：C2 攻防（agent 自 ack 被拒→human ack 放行）、push 分歧 pull --rebase 恢复、四机 converge 一致性 |
| `dogfood-s7-chaos.sh` | 同拓扑 + 第五机 mobile-agent | 12 突发：需求变更/事故插队/同单跨机冲突/机器损毁 re-clone/新成员移交/误取消重开/competing contracts/依赖被砍/伪造证据/紧急回滚/虚报被抓/优先级重排 |
| `dogfood-s7-chaos2.sh` | CMS 五机 | 12 全新突发：发版跳版/agent 失联重派/breaking 三连攻防/假冒 --actor（git 审计揪出）/手改损坏 strict decode 拒+revert 恢复/依赖环/撞 ID/8192 门/裸修中央仓/批量雪崩/重工循环/诚实框架假证据 |
| `dogfood-s7-design.sh` | 三机 | 共识层第四幕：设计发起/轮次发言 pending 派生/超前曝光 warning/agent 自 decide 被拒/supersede 继任链/waiting-for 巡检 |

**增长判据（P4，2026-09-18）**：新剧本 = 真实事故/推演复盘驱动，禁预防性编写。现有四剧本均对应真实推演裁决；chaos3/chaos4 预防性编写的诱惑会随团队信心增长，此判据防剧本线性增长税。

---

## 6. 复盘教训集

- **2026-09-12 第一课（新字段四路）**：priority/due_on 曾漏接 `context me`——只进存储不进信道 = 没上绳，人拍板牛不见。机械锁 `TestWorkPlanningFields`。
- **2026-09-12 第二课（嵌套 aggregate 同罪）**：contract 进了存储但 list/kanban/WorkBrief 三路全盲——接口标准是基石唯一钉死之物，恰好漏得最狠。机械锁 `TestContractSignalFourPaths`；WorkBrief 构造收敛至 `newWorkBrief` 单点。
- **2026-09-13 语义轴推演**：C2 的 human 型校验从 v1 非空检查升级为 v0.3 写入路径（`workspace.validateRefs`）+ converge 审计双层。
- **2026-09-14 S7 混沌发现**：open 项依赖目标 cancelled = 永不满足（missing 可能重现，cancelled 不会）→ converge 硬阻塞 `dependency X cancelled (unsatisfiable)`；done 项历史残留不阻塞；修在 converge 层（Index.isDone 是历史残留语义，勿动）。锁 `TestDependsOnCancelledBlocks`。
- **2026-09-16 rdc-05 事故**：适配器 instructions 对象数组致 opencode 启动失败；插件放 `.opencode/plugin.ts` 致静默失效。修复 = 适配器资产 CLI 内嵌（go:embed），升级二进制后每个代码仓重跑一次 `adapter install`。
- **2026-09-18 二次 review 教训**：同语义门必须覆盖全部写径——active 门扩三态时只查了 Service.Update，漏了 migrate 的 repo 直写（resolve-owner role-only 绕道）。双锁：workspace 门 + resolve-owner 门。

---

## 7. 字段移除轴裁决（2026-09-18，P2）

**问法（R9 扩展到 WorkItem）**：逐字段问"删了它，LLM + git + 现有机制顶得住吗？顶不住的必须回答'卡哪个门'或'引哪条导'"。新标准：**字段辩护 = 被某个门消费（卡），或被判为方向信号（引导）并写明辩护词；四路展示不是充分辩护**。

| 字段 | 卡点/引导 | 裁决 |
|---|---|---|
| status / revision | 状态机 + CAS | 留（卡） |
| system / assignee | 可寻址/可问责门 + active 门 | 留（卡） |
| contract / evidence / human_ack | C1 / C2 | 留（卡） |
| accountable_human / detected_by | 问责链（F4 / bug 溯源） | 留（卡） |
| description / target_version | 可判定性门（类型最小信息集） | 留（卡） |
| title / type / schema_version / depends_on | 可读性 / 类型门 / 严格解码 / 依赖审计 | 留 |
| progress | 收尾信号（reported，basis 必填） | 留（引导） |
| acting_role | 问责上下文（work assign 消费） | 留（引导） |
| migration_status / legacy_owner | v1→v2 过渡件，resolve 后自清 | 留（过渡） |
| priority / due_on | **无门消费**；PM 分诊方向信号，四路已接通 | **留（引导）**：辩护词 = 分诊优先级的方向信号，弱但真实；删除成本（迁移+四路拆除）大于保留成本。若真实使用中无消费（PM 从不看），下轮移除轴再审 |

**结论**：字段集全部通过，但"字段只增不减"的趋势由本裁决封口——新增字段必须声明卡点或引导职能（AGENTS.md 铁律）。

---

## 8. 越位事故与身份裂缝裁决（2026-09-20，TASK-007）

**事故**：226（human 型 actor，PM）的 AI 会话自建单 TASK-007 → 自侦察生产集群 5m46s（kubectl/helm/registry）→ 自挂证据 → 自改派。绳上全程合法。靠 human 扫视才发现。

**根因三层**：
1. **身份层裂缝（最深）**：绳的信任模型是"actor 类型 = 键盘后是谁"。多机部署下，human 型 actor 的机器上跑 AI 会话 = AI 披 human 皮，所有类型门（C2 ack / design decide / F4 授予）对其失明；activity 审计无法分辨人还是 AI（问责链作废）。
2. **语义层边界（设计使然）**："侦察算执行"是语义分工判断，绳的卡点不变式明写不卡语义。越位被拦靠 human 扫视——这是设计闭环，不是绳失灵；**失灵的是"越位后绳上无痕迹"**（= 层 1）。
3. **结构层放大器**：中枢机 AI 手握生产权限 + 单子自建 + human 身份零卡点；且 assignments 的 executor 图在事故前无任何消费者（纯装饰）。

**裁决**：
- **human 挂 active 执行单 assignee = warning「决策位坐执行位」，不设硬门**——单人项目 human 亲自干活是合法常态，硬门会误伤；多机 PM 场景靠曝光让人扫视有信号（分层强制镜像：硬门挡"无主"，曝光挡"错位"）。
- **active 单 assignee 在该 system 无 executor assignment = warning「跨域执行」**——仅当该 system 已有 executor 图（存在行）才查，空图 = 未排班不查（避免早期项目噪音）。executor 图自此有第一个消费方。
- **PM 机 AI 会话必须用独立 agent 型 actor**（responsible-human = 该 human，F4 门通过），human 型 me 只留给终端亲操；`adapter install --actor` 落 AI 身份进 ousheng.json。此为车队纪律非产品门（同机多窗口协议 join 已有，无需新代码）。

**反事实**：若把两条警告升级为 BLOCKER——单人项目（human 唯一执行者）永远 BLOCKED，converge 信号失去分辨力，重蹈"waiting 算 BLOCKED"覆辙；若不修身份层只修曝光——下次事故形态变为"agent 型越权执行但账面干净"，曝光照旧抓不到，故车队身份纪律与产品曝光缺一不可。

**锁**：`TestHumanAssigneeActiveWarns` / `TestCrossDomainExecutorWarns` / `TestAdapterInstallActorOverride`。

### §8.1 本体覆盖矩阵（2026-09-20 系统性推演）

本体四层——词汇（严格解码）/ 类型（actor.type、responsibility）/ 门（写路径）/ 审计（converge 派生谓词）——**无第五层推理机**：没有任何组件从本体推出"这个动作违职责"，converge 是批量事后审计不是动作时推理。

四公理：A1 绳只能治理流经它的写；A2 卡状态合法性不卡语义（职责=语义）；A3 actor.type 语义 = 键盘后是谁（部署假设，非验证事实——绳不验也不能验）；A4 system 是逻辑域非物理机。

职责覆盖三档：
- **写路径硬门档（真 100%）**：C1/C2/F4/active 门/状态机/问责 human 型/assignee 存在性——只覆盖"跨主体伤害与状态合法性"
- **事后曝光档**：决策位坐执行位 / 跨域执行（ee6b8e9 起）
- **0% 档（原理不可修到 100%）**：绳外动作（A1）与语义分工判断（A2）——靠人扫视，这是牵引绳的本体论决定不是 bug

职责遵循的物理上限 = 流经绳的写 100% 卡死 + 流经绳的痕迹 100% 曝光 + 身份真实（车队纪律）。

### §8.2 待裁缺口三件（记判据防丢）

| 缺口 | 现状 | 判据（真实出现才动） | 候选修法 |
|---|---|---|---|
| 机器↔演员绑定缺失 | git committer 从未与 actor 做交叉验证；AI 穿号只靠车队纪律 | 再现 AI 穿号事故，或同机多操作源需区分且 committer 面不够用 | git committer × actor 交叉验证（converge 审计档起步，不进写路径） |
| 演员命名退化（机器号即演员） | 1/224/225/226 把机器/会话/身份三合一；schema 合法但 A3 假设被打穿（老工作区 tide-ousheng 反有语义演员名，换仓时退化） | 多项目共用中央仓撞号，或审计真实需要区分"人/机/会话" | 重注册语义化演员 id + migrate；成本高，等真实疼点 |
| 系统组件驻留关系未建模 | 网关 = mlake 合约 × 226 设施；TASK-007 跨域警告噪音是其影子 | 跨域警告造成真实误判（忽略或错误改派），或第三次出现"合约域≠设施域"单 | system 元数据加 residency 字段——须按 §7 铁律声明卡点/引导职能 + 四路验证 |

**2026-09-21 状态更新**：实栈重建已采用语义演员名（`data`/`mlake`/`ui`/`test`/`pm`，见 §15 RC6），本项目内的命名退化已消除；判据（多项目共用中央仓撞号）仍是跨项目缺口，条目保留待真实疼点。

---

## 9. 设计撤回终态裁决（2026-09-20，atw-approval-notify 事故）

**事故**：PM 取消 MQ 推送需求要撤回 draft 设计，发现** draft 无终态**——supersede 只收 agreed（draft 无权威可转移），唯一出路是手改 frontmatter 贴「已撤回」横幅 + 手切 related_items 消超前曝光警告，被迫绕绳两次（d05ea98/571d573）。无合法路径的绕过比有门的绕过更糟：它把「手改 frontmatter」常态化。

**裁决**：
- 补第四态 **withdrawn**（撤回终态）：`design withdraw <topic> --actor <owner-or-human>`。
- **这不是拍板门**（与 decide/supersede 的 human 门区分）：draft 从未获得权威、无共识可转移，收回自己的提案不构成跨主体影响——owner 即可操作（agent 可撤自己的）；非 owner 的 agent 撤别人的 draft = 跨主体伤害，拒（镜像 F4 哲学）；human 手不卡。
- 字段铁律：withdrawn 须 `withdrawn_by`、禁 `superseded_by`（无权威可转移）与 `decided_by`（从未拍板）。
- 联动零改动即生效：pending_reviews 与 converge 超前曝光都只认 `draft`，withdrawn 结构性出局——226 当日手切 related_items 的动作从此不需要，死方案持活链接属档案事实不是噪音。

**反事实**：若允许 agreed→withdrawn——已获共识的方案被 owner 单方面静默收回，下游按共识开工的工作失去权威依据且无继任者指认，重蹈「知识断链」；故 agreed 的关闭必须走 supersede（human 门 + 继任者存在）。若 withdraw 也设 human 门——agent 发起的 draft（PM 发单流程 design 先行，agent 可起草）被废弃时同样无路可走，回到手改横幅。

**锁**：`TestDesignWithdraw` / `TestDesignWithdrawnValidation` / `TestWithdrawnDesignNoAheadWarning`。

---

## 10. sync 收尾推送与协议段命令速查（2026-09-20，BUG-001 上绳事故）

**事故**：226 的 ui 会话发现 mlake 问题（Flink REST NodePort 31083 间歇超时），发单全程证明绳在工作——plugin 正常 sync、MCP `query_work_items`/`report_bug` 调通、BUG-001 连证据一起落地（detected_by: ui）。但用户观感是「没上绳，ousheng 没任何反应」。解剖出两个真缺口：

1. **CLI 语法不可发现**：AGENTS.md 协议段只讲三时机不给命令表。AI 猜 `ousheng bug report`（语法其实对）→ 见 usage 退回 → 疑心 → `--help`/`-h`/grep 二进制/sqlite 挖表，绕两分钟最后靠 MCP 工具发单。协议给了时机没给词汇，等于指了方向没给缰绳。
2. **收尾不推送**：`ousheng sync` 只 pull 不 push，plugin 时机③只跑 converge。BUG-001 自动提交后烂在 226 本地（ahead 2），全舰队不可见直至人工推送。多机车队里未推送的工单 = 不存在的工单。

**裁决**：
- `sync` 在 pull 成功后 **push 收口**（先拉齐再推，分叉态不推）；软失败——push 失败仅提示不阻塞索引与上下文，无未推提交静默。推送不进写路径（写路径 push 会在冲突时阻塞合法写入，违反「卡 agent 的写不卡 human 的手」的镜像边界）。
- AGENTS.md 协议段补**命令速查**（bug report/todo/work list/show/update/assign/context me/converge→sync），机器中立禁串不变（锁 `TestAgentsSectionIsMachineNeutral`）。命令词汇是绳的缰绳不是牛的活——协议层引导，非语义代办。
- 时机③ 文案补 `sync` 推送收口；plugin 不动（session.idle 每次响应后都触发，sync 含网络操作挂上去就是每 loop 轮询，违反三时机铁律；时机① 的 sync 已保证下一会话启动时兜底推送）。

**反事实**：若 push 进写路径——两机并发写后先后 push，后推者被拒，写路径报错但 YAML 已落盘，用户面对「单建了但命令失败」的中间态；sync 收口把推送的冲突面收敛到 pull 之后，失败可重试无副作用。若命令速查放进 plugin 注入——每 session token 开销，且 AGENTS.md 本就是 opencode 的 rules 载体，零额外成本。

**锁**：`TestCLISyncPushesLocalCommits`（事故形态复现：写路径自动提交 → sync → 远端必须收到）。

**关联**：LiteLLM 网关（192.168.1.12）间歇断连同日志可见——基础设施问题，另案，不在绳上修。

---

## 11. 工作区全局放行裁决（2026-09-20，external_directory 权限询问事故）

**事故**：224/225 每次 opencode 启动都弹 `/data/kanban`（工作区）访问权限询问。根因三层：opencode `external_directory` 默认 `ask`；TUI 的 "always" 放行**只活当前会话**（不落盘）；工作区在代码仓之外必然触发外目录门。

**裁决**：`adapter install` 第六步——把 `permission.external_directory[<workspace>/**]="allow"` 合并进**全局配置** `~/.config/opencode/opencode.json`。要点：
- **落点只能是全局**：工作区路径是机器本地信息，五件套全是入库机器中立资产放不下路径；项目配置层（根/`.opencode/` opencode.json）入库会跨机冲突。
- **合并只增不删**：key=工作区路径模式（自标识），既有键全保留；首次改动前备份 `.bak-ousheng`；`permission`/`external_directory` 非对象结构或 JSON 解析失败（JSONC 等）→ 拒绝改写、给人话指引——**不碰用户配置**；软失败不阻塞 install（权限询问是摩擦不是阻断）。

**反事实**：若把路径塞进项目 opencode.json——224 是 `/data/tide/kanban`、225 是 `/data/kanban`、本机是 `/Users/...`，同一仓克隆到别机即冲突，重蹈「协议外置分发 = 漂移死法」；若不修——每个会话人工放行一次，零摩擦上绳的承诺破产，用户迟早习惯性点掉所有权限弹窗（安全训练负向）。

**实证**：`opencode debug config` 确认 `.opencode/opencode.json` 确实被加载（文档 precedence 清单未列，1.18.31 实测有效）——五件套 MCP 配置不白写；权限解析为空印证默认 ask。

**锁**：`TestAdapterInstallGrantsWorkspacePermission`（预置 provider 合并保留 + 幂等 + 非对象结构软失败）。既有六个 install 测试补 `t.Setenv("HOME")` 隔离——install 现在写全局配置，不隔离会污染真实机。

---

## 12. doctor 部署标准自检转正（2026-09-20，延后清单判据实锤）

**判据触发**：延后清单记的 `ousheng doctor` 激活判据是「多人反复踩 repo set 忘配/路径失效」。本日连续三起实锤：每会话权限弹窗（§11）、td 旧座位残留幽灵 actor、226 旧工作区 colocated 配置漂移——标准已成形（五件套+全局放行+单二进制，每机 4 处机器本地状态）但没有机器可查的验证，等于靠人肉背标准。

**裁决**：
- `ousheng doctor [--dir <代码仓>]` 三层检查：机器级（版本/MCP）/ 座位级（ousheng.json 身份注册态、插件逐字节、协议段版本、全局放行）/ 工作区级（me 注册态、repo 映射有效性）。
- **检查不修复**：修复单一入口 = `adapter install`（幂等）或对应 registry 命令；FAIL 必带 `→` 修复命令，任一 FAIL 退出 1（可脚本化）；WARN 不影响退出码（MCP 缺失只降通道不降功能）。
- 检查口径对齐写入口径：plugin 逐字节（install 恒同步语义）、opencode.json/package.json 只查存在（install 保留语义）、全局配置解析失败/非对象结构降级 WARN（不碰用户配置原则，§11）。
- 幽灵 actor 检查（ousheng.json actor 未注册）= TASK-007 越位事故与 td 残留的机器可查形态。

**反事实**：若 doctor 顺带修复——修复逻辑两份（install + doctor）必然漂移，且静默改用户全局配置违背 §11；若只报不给修复命令——用户还得翻文档，等于把标准从人肉背改成翻手册查。

**锁**：`TestDoctorHealthySeat` / `TestDoctorDetectsDrift`（插件漂移/幽灵 actor/放行缺失/repo 映射失效四类）/ `TestDoctorMachineOnly`。

---

## 13. 座位资产入库策略裁决（2026-09-21，td 清理事故）

**事故**：清理 td（退役的 ui 座位）时发现座位资产（plugin / opencode.json / AGENTS.md 协议段）早已提交进**公司仓** datax-ui.git。后果两层：同事 clone 后开 opencode，插件加载但无 ousheng.json——v2「失败必须大声报错」的设计把个人工具链的失败噪音广播给公司仓的每个克隆者；协议升级时每个座位的 worktree 必脏，谁提交谁推成了无规则的灰色地带。

**裁决**：
- `.opencode/` 座位资产**全部走 gitignore**（ousheng.json / opencode.json / package.json / package-lock.json / bun.lock / node_modules/ / plugins/），由 adapter install 幂等维护 `.opencode/.gitignore`。
- 理由：资产由**二进制内嵌分发**（adapter install 即写出）——入库副本必漂移，与「协议外置分发 = 漂移死法」同源；单一来源只能是二进制。
- **例外**：AGENTS.md 协议段维持入库——机器中立（锁 `TestAgentsSectionIsMachineNeutral`）、团队可见、doctor 可查版本；协议升级后各座位重跑 install，改动谁看见谁提交。
- 一次性清理：已入库副本需 `git rm --cached`（gitignore 不回溯已跟踪文件）。

**反事实**：若连 AGENTS.md 也忽略——仓库自带的 AGENTS 正文会被整文件忽略，且协议对团队不可见，绳的可见性退化成一个只在本机生效的隐藏配置；若维持全入库——个人工具链的失败噪音持续广播给全公司仓克隆者，且「入库副本 = 分发副本」的漂移面永久存在。

**锁**：`TestAdapterInstall` 的 .gitignore 断言扩展（七行资产全覆盖）。

---

## 14. 双写入口语义漂移与审计空洞裁决（2026-09-21，实栈审计）

**事故**（实栈逆向推演取证）：
1. `unknown.jsonl` 4 条 `evidence_added` 且 `actor:""`（旧仓同病 = 长期存在）。根因链：`cmd/mcp` 六个写工具把 actor 当 `omitempty` 可选输入、空值直接透传 service → `gityaml.activityPath` 对空/非法 actor 静默归入 unknown 桶。**CLI 的 `evidence add` 有守卫、MCP 没有——同一写语义两套实现。**
2. MCP `add_evidence` 的 git_commit 验证用**工作区仓**（`in.Path`），CLI 用 `repos.yaml` 映射的**系统代码仓**——同源漂移，证据可能在错的仓里"验证"。

**裁决**：
- **一语义一实现**：机器本地配置（repos.yaml / me / 座位身份）与身份解析收进 `internal/machine`，CLI 与 MCP 共用同一实现（CLI 侧保留薄委托以不动调用面）。
- **身份解析链**（两个入口同一条）：显式 `--actor`/`actor` > 座位配置 `.opencode/ousheng.json`（AI 窗口身份，plugin 同读此文件）> 工作区 `.ousheng/me` > **拒绝**。座位优先于 me：me 可能是 human（PM 机终端身份），AI 的写不得默认归到人名下。
- **写路径身份硬门**（`workspace.requireActor`）：六个写方法（Create/Update/ReportProgress/AddEvidence/Ack + Assign 委托）入口一律校验——空 actor 拒（fail-closed，不许静默落 unknown 桶）；注册表非空时 acting actor 须已注册。activity 是审计线索，不可归属的写等于审计空洞。
- **git_commit 证据定位**：两入口统一走 `machine.RepoDirForSystem`（映射 > 工作区自身 monorepo 回退）。

**反事实**：若只在 MCP 层加解析——另一条未来入口（Web/其他宿主）复发；若只在 service 层加校验——AI 的调用会以难懂的错误失败（体验差、易被绕过地"随手补 actor"），且 git_commit 定位漂移依旧。两层各自解决不同问题：入口层解决"能正确归属"，service 层解决"绝不允许不可归属"。

**锁**：`TestWriteRequiresActor`（空/未注册/已注册三态 + evidence/progress 路径）、`TestMCPRejectsUnresolvedActor`、`TestMCPResolvesActorFromWorkspaceMe`、`TestMCPResolvesActorFromSeatConfig`、`TestResolveActorPriority`、`TestRepoDirForSystem`。

---

## 15. 实栈审计：定位、根因与铁律总表（2026-09-21）

**定位**：OuSheng 仓 = 产品（规则/铁律/裁决的唯一权威）；224/225/226/本机 = **实栈验证场**（跑真活、暴露真缺口），不是产品的一部分，也不为产品掩饰边界。事故的唯一合法归宿链：**实栈发现 → 产品裁决（本档案）→ 机械锁 → doctor 可查 → 二进制升级 → 实栈重跑**。没走到"锁"的事故 = 没修完。方向锚 = 卡点不变式（卡 agent 的写、不卡 human 的手；卡跨主体伤害与状态合法性、不卡语义与质量）——实栈的痛不构成扩权理由。

**最深根因**：OuSheng 用自己的标准管别人，却没用同样的标准管自己的部署。产品对"被管工作"要求可寻址/可问责/可判定 + 卡点不变式；而"产品自己怎么落到 N 台机器上"此前只活在文档与人脑里。第一批真机 + 真人 + 真公司仓，一次性暴露三层缺口：**分发面**（资产怎么到机器、什么形态）、**环境面**（PATH/HOME/env/remote/版本/权限/并发——单机 temp 仿真覆盖不到）、**身份面**（谁在写、以什么身份、谁能授权）。

**六类根因与铁律**：

| # | 根因 | 铁律 | 强制手段 | 状态 |
|---|---|---|---|---|
| RC1 | 状态分层未定义（团队共享 / 机器本地 / 仓库中立资产混一） | 每种状态一个权威来源，形态匹配其变化频率与共享范围 | 二进制内嵌 + 五件套 + 全局放行 + gitignore 策略 | 闭环（§11/§13） |
| RC2 | 同一语义多份实现 | 一语义一实现；多入口共享内核，入口只做参数适配 | `internal/machine` 共享 + requireActor | 已修，补双入口对照锁 |
| RC3 | fail-open 默认（不确定时静默降级） | 不确定即拒绝；关键动作结构化，不靠纪律 | requireActor / AppendActivity 校验 / sync push / 拍板纪律 | 本次收口 |
| RC4 | 仿真保真度不足（单机仿真 ≠ 真机环境） | 实栈是验证场；每类环境差异须有对应验证手段 | dogfood（流程面）+ doctor（配置/环境面） | doctor 补三项；真机剧本另轮 |
| RC5 | 生命周期/终态不完整 | 每对象有完整生命周期；每个部署可安装/可验证/可升级 | withdraw（§9）+ doctor（§12） | 闭环 |
| RC6 | 身份三轴混一（me / 座位配置 / actor type） | 身份显式解析、单一来源、**类型即授权** | 座位单一来源 + 解析链 + converge 越位曝光 | 语义命名已改善；命名退化缺口留档 §8.2 |

**统摄判据（自举）**：让 OuSheng 的部署自己满足自己的发布三条件——部署单元可寻址（哪仓哪机）、配置可问责（谁配谁改）、验收可判定（doctor 验）。

**正向对照**：写入门禁 / CAS / S5 等价 / 注入面最小化 / 拍板门在四机真活下零事故；TASK-007 是身份轴被穿号绕过（已由身份修复堵上），不是门失效。

**本次收口的三项机制**：AppendActivity 拒绝不可归属 actor（审计要求每个写可归属）；doctor 补三项检查（座位资产是否入库 / unknown 审计桶 / 工作区水位分叉）；plugin 空闲提醒未推提交（收尾 sync 的结构化兜底）。

---

## 16. 实栈遗留清理与 sync 分叉指引（2026-09-21）

**背景**：§15 审计的遗留四项收口。

**裁决**：
1. **sync 分叉指引**：`git pull --ff-only` 撞非快进（两端各有未合并提交）时，sync 打印人话指引 + 修复命令（`git -C <ws> pull --rebase` 后再 sync），不再泄漏裸 git 报错。**sync 绝不自动 rebase**——chaos 剧本实证同单冲突（P0 vs P2）是人的决策，后台/插件自动 rebase 会把工作区卡进中途态。不推分叉态、不动本地提交。锁 `TestCLISyncDivergenceGuides`。
2. **历史 unknown 桶迁移**：`unknown.jsonl` 4 条（2026-09-21 前）为已修复审计洞的历史记录，重命名为 `unknown.jsonl.fixed-20260921`——数据不入垃圾堆（审计可查），doctor 的新写检测（按 `unknown.jsonl` 名）不再被历史常显 WARN 干扰（常显告警 = 告警疲劳，反向削弱工具公信）。
3. **残留清理**：225 的 `docs/board-protocol.md`/`docs/context-protocol.md` 未跟踪副本删除——协议文档权威在 OuSheng 仓（AGENTS.md 协议段已声明），副本即漂移面。
4. **§8.2 命名退化**：本项目已由重建采用语义演员名，状态更新入 §8.2。

**反事实**：若保留 unknown 桶原名——doctor 每次运行一条永久 WARN，人逐渐无视 doctor 输出（本次 doctor 当场抓插件漂移的价值会被噪音稀释）；若直接删除——审计记录双删（活文件 + 有意抹除），违背"审计可查"；重命名是唯一同时满足"可查"与"不误报"的形态。

---

## 17. 验证分层裁决：不为已锁机制再造脚本（2026-09-21）

**背景**：§15 遗留「缺口 5」原计划补真机环境面剧本。逐项核对后改判。

**裁决**：验证三层分工，各守其界——
- **机制面 → Go 锁**（进程内、离线、可 CI）：身份解析/写路径硬门/doctor 各项/sync 分叉/plugin 真编译。今日核对：环境面九类事故对应机制已全部有 Go 锁。
- **流程面 → dogfood 剧本**（多机拓扑仿真）：四机全流程/混沌两辑/共识层。并发写、同单冲突、撞 ID 已覆盖。
- **真机面 → 实栈验证场**（224/225/226/本机）：opencode 会话里的插件实际行为、权限弹窗是否消失、AI 能否顺畅走完动线——本质无法离线仿真，只能由实栈暴露。

**禁**：为已被 Go 锁覆盖的机制再造 shell 剧本。冗余验证层会腐烂（脚本与实现双份维护、失败时不知信谁），且稀释"新剧本 = 真实事故驱动"的纪律（§5 场景清单）。

**反事实**：若照原计划写环境面剧本——它只能在真机上跑（含 IP/路径），违反产品仓机器中立；退化为单机仿真则与 Go 锁重复。两种形态都不成立，故不写。

---

## 18. 尺寸门溢出路径与超限曝光（2026-09-21，BUG-002 实测）

**事故**：BUG-002 累积到 10763B（> 8192）→ 所有写被拒（连证据都加不上），且**只有下次写才暴露**——单被静默冻结。用户对着错误信息无路可走。

**裁决**：**门保留**（骨架原则：工作项是骨架+指针，与 `forbiddenContent` 禁代码块/堆栈同源；工作项不是文档仓库。调大 = 把撞墙推后且骨架原则失去唯一执法者；改警告 = 失控增长）。补两处：
1. **尺寸门报错带溢出路径**：`证据用指针（--type document --locator <路径/URL>），正文归外部文档；或先瘦身本条`——fail-closed 也必须给下一步（同 sync 分叉指引、命令速查缺失一类教训）。
2. **converge 曝光超限项**（写冻结）：超限是硬门造成的静默态，必须进曝光层（硬门 + 曝光两层设计）。落命令层而非 `converge.Check`——Check 只依赖索引（S5），文件尺寸在命令层取。

**锁**：`TestValidateWorkItemSizeGate`（报错含溢出路径）、`TestCLIConvergeExposesOversizedItem`（曝光可查）。

**数据迁移**：BUG-002 长文外移 datax 仓 `docs/`，工作项改挂指针证据（原文不改、可审可归），走 `work update --file` 合法路径瘦身解冻。

---

## 19. 一台机多座位裁决（2026-09-21，test 迁 226）

**背景**：test 从本机迁到 226（与 ui 同机，`/data/ui` + `/data/test` 两个 opencode 窗口各穿一个身份）。

**结构基础（都已支持）**：座位身份按目录（`.opencode/ousheng.json`）；工作区机器级共享；全局放行按工作区；注册表与 actor 不随机器走。

**缺口**：CLI 的默认身份解析只看机器级 `me`（`--actor` > `.ousheng/me`），不认座位目录——同机两座位时，非 me 的窗口裸命令会**归属错**（身份合法、类型对，只是写到了别人名下，比 TASK-007 更隐蔽）。MCP 侧已修（§14）。

**裁决**：
- CLI 写路径默认身份 = **cwd 座位配置 > 工作区 me > 拒**，复用 `internal/machine.ResolveActor`（与 MCP 同一实现——一语义一实现）。单座位机 `me == 座位身份`，行为不变。
- 本机（test 迁走后）改为 **me=pm（human，终端拍板）+ 窗口 `--actor pm-dev`（agent，resp=pm）**——human 型 me 只留终端（226 同款形态）。
- 协议段速查措辞随之更正（"身份缺省 = 本目录座位身份，无座位则本机 me"）。

**反事实**：若只靠协议让 AI 每次带 `--actor`——纪律依赖，漏一次就是一笔错归属的审计记录；若给每个座位单独 `me`——me 是工作区级文件，物理上做不到。

**锁**：`TestCLIActorPrefersSeatConfigOverMe`（座位优先 + 无座位回退 me）。

---

## 20. 关系梳理：人 — agent — 角色 — 系统（2026-09-21 梳理，决策搁置待判据）

**背景**：用户提出目标形态——多项目/多人/多角色/多 agent/多系统，并给出五条关系：项目×人 1:n、人×agent 1:n、agent×系统 n:1、项目×系统 1:n、人×角色 1:1。本轮**只梳理不拍板**，决策全部搁置（判据见文末）。

### 20.1 代码事实（当前实现）

| 实体 | 字段 | 要点 |
|---|---|---|
| Actor | `id, type(human\|agent), display_name, responsible_human?` | 人与 agent 同表；agent 必须挂在册 human 名下（F4 门强制） |
| System | `id, name, parent?` | 项目内系统表，可层级 |
| Role | 字符串 | 只活在 `assignment.role` / `work_item.acting_role`；`roles.yaml` **半实现**（当前只登记 fullstack，backend/tester 未登记——team add 不建登记，只有 todo 路径建） |
| Assignment | `actor × system × role × responsibility` | agent 与系统的挂载点 |
| WorkItem | `system(单), assignee(单), acting_role, accountable_human, detected_by, depends_on` | 单系统单主 |
| Design | `systems[], owner, related_items[]` | 跨系统需求的唯一合法载体（后拆单） |
| Project | `id, name` | 一个工作区 = 一个项目 |
| 机器 | 不入模型 | 座位 = 代码仓目录 + 工作区指针 + 窗口身份；repos.yaml 是 system→代码仓映射。**actor 与机器解绑**（test 搬家实证：只搬座位，注册表不动）✅ |

### 20.2 当前 fleet 实测关系

```
项目 tide-datax ─┬─ 系统 data / mlake / ui
                 └─ 人 pm(唯一 human)
pm ─┬─ agent data   → 系统 data (backend)
    ├─ agent mlake  → 系统 mlake (backend)
    ├─ agent ui     → 系统 ui (fullstack)
    ├─ agent test   → 系统 data/mlake/ui (tester ×3)   ← n:n（对目标 n:1 的偏离）
    └─ agent pm-dev → 无系统主属（PM 机窗口）
机器：224=data 225=mlake 226=ui+test 本机=pm-dev
```

### 20.3 五条目标 × 现状

| 目标 | 现状 | 差距 |
|---|---|---|
| 项目 1:n 人 | 注册表按工作区隔离 | 同一个人跨项目要重新注册；全局注册服务已判死法 → 只能"同名 id 各项目注册"约定 |
| 人 1:n agent | ✅ responsible_human + F4 | 无 |
| **agent n:1 系统** | ✗ 现状 n:n（test 三系统） | 待裁（见 20.5） |
| 项目 1:n 系统 | ✅ | 无 |
| **人 1:1 角色** | ✗ 无 人↔角色 绑定 | 待裁（见 20.5） |

### 20.4 实战事故 → 压到的关系（全部已修，除标注）

| 事故 | 关系 | 状态 |
|---|---|---|
| 机器号当演员名（1/224/225/226） | 机器维混进身份 | ✅ 已语义化（data/mlake/ui/test/pm/pm-dev） |
| TASK-007 越位（AI 穿 human 号） | 人/agent 边界、类型即授权 | ✅ responsible_human + 窗口身份 + 类型门 |
| pm 错配（25 条归因失真） | 机器 me vs 窗口身份 | ✅ 座位解析链（§19） |
| test 挂三系统 | agent n:n | ⏳ 权宜（消跨域告警），待裁 |
| test 换机器 | actor 是否绑机器 | ✅ 解绑正确 |
| 加/删角色 | 角色生命周期 | ⏳ 半实现 + 删无路径 |

### 20.5 搁置的两个议题（含判据，触发即取用）

1. **agent↔系统 n:1 严格化**（重点：test 跨三系统的处置）。三选项：严格拆席（test-data/test-mlake/test-ui）／主系统 + 授权跨域（推荐候选：主系统定归属与告警基线，assignment 表已授权协作，converge 区分已授权 vs 未授权跨域）／维持 n:n。
   - **判据**：多 agent 多系统真实出现归属或告警歧义（跨域告警误判、审计需要按系统切分 agent、或第二个跨系统角色出现）。
2. **人↔角色 1:1 结构化**（human 加 role 字段 或 roles.yaml 关系表；拍板门可校"必须该角色的人"）。
   - **判据**：多人协作真实发生（需要"谁是 pm/后端"可查），或拍板门需要按角色校验时。
   - **附带（小缺口，可独立提前修）**：`roles.yaml` 半实现（team add --role 不登记、删角色无路径），判据：下次动角色相关代码顺手补全 + 补锁。
3. **多项目人身份约定**（同名 id 各项目注册）。
   - **判据**：第二个项目上绳时。

**结论**：当前实现支撑"单项目/单 human/多 agent/多系统"绰绰有余；目标形态（多 human/多项目）的两处结构缺口已定位并记判据，不预先设计（避免为未出现的痛造机制）。

---

## 21. 自动收口上绳与上绳边界（2026-09-21，实栈两条反馈）

**反馈一：经常不上绳**（本地有 commit 未推）。实锤两起：BUG-015 烂在本机、BUG-002 早先烂在 226——收口之前依赖"AI 记得跑收尾 sync"（纪律依赖）。

**裁决**：
- plugin 空闲时**自动收口**：本地 `rev-list @{u}...HEAD` 检查（零网络）→ **有未推/落后才动网**（pull+push）。不做无条件每 idle sync——idle 每次响应都触发，会退化成每响应一次网络往返。
- **不确定 = 交互确认**：分叉/推送失败 → error 级大声报错 + 指引（人处理，不自动 rebase，§16）。
- 会话启动的 sync（时机①）保持。

**反馈二：自域 bug 不必上绳**。data 执行任务时发现并顺手修好自己域内的 bug——此时上绳 = 无接收方的仪式（自建、自派、自证、自关）。

**裁决（上绳边界）**：
- 上绳服务于**跨主体协作**（与卡点不变式同源：卡跨主体伤害与状态合法性，不卡语义与质量）。
- **自域**（同系统、自己顺手修、不触对外接口）→ 记入当前任务证据/进度（`evidence add <当前单>`），**不另开单**。
- **跨系统 / 跨 agent / 需人拍板 / 触及对外契约** → **必须上绳**（C2 门不豁免——契约是跨主体接口，"自域"不成立）。

**残余风险（如实记录）**：自域修复不落独立单 → PM 视野降低（缓解：仍记当前单证据 + 代码提交可查）。**判据**：若某笔自域修复事后被证明需要协作且已丢失 → 收紧本规则。

**锁**：`TestPluginReadsMachineConfigAndFailsLoud`（自动同步 + 分叉报错内容）；plugin 真编译锁（`TestPluginCompiles`）。
